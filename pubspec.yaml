name: quickactions_example

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  quick_actions: ^0.4.0+8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true